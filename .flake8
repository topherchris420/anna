[flake8]
# Match Black's configured line length (see [tool.black] in pyproject.toml) so
# the formatter and linter agree instead of flake8 defaulting to 79. Black owns
# line length, so E501 is ignored for the few long strings/URLs it can't wrap.
max-line-length = 80
extend-ignore = E203, W503, E501
